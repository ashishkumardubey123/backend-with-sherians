<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-black text-white">
    <div class="container mx-auto py-8">
        <h1 class="text-3xl font-bold mb-6 text-center">Edit Post</h1>
        
        <!-- Form for editing the post -->
        <form action="/update/<%= post._id %>" method="POST" class="space-y-6">
           
            <div>
            <textarea id="content" name="content" 
                          class="w-full px-4 py-2 text-yellow-100 font-semibold  bg-gray-700  rounded-md focus:outline-none focus:ring-2   focus:ring-indigo-600" required><%=post.content %></textarea>
            </div>  
            <div class="flex justify-end mt-3 space-x-4">
               <a href="/profile" class="bg-red-500 hover:bg-red-700 font-semibold px-4 py-2 rounded-md shadow-lg transition duration-300">Cancel</a>
               <button type="submit" class="bg-yellow-500 hover:bg-yellow-700 px-4 py-2 rounded-md shadow-lg transition text-black font-semibold  duration-300">Update</button>
           </div>
          
        </form>
      
      </div>
    </div>
</body>
</html>
