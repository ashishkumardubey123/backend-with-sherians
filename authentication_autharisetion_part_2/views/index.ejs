<!DOCTYPE html>
<html lang="en">
<head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <title>Create User</title>
     <script src="https://cdn.tailwindcss.com"></script>
     <style>
          body {
               background: linear-gradient(135deg, #1e293b 0%, #2d3748 100%);
          }
     </style>
</head>
<body class="flex items-center justify-center min-h-screen">
     <div class="w-full max-w-lg p-8 bg-zinc-900 rounded-xl shadow-lg">
          <h1 class="text-4xl font-bold text-center text-white mb-6">Create User</h1>
          <form action="/create" method="post" class="space-y-6">
               <!-- Username Field -->
               <div class="relative">
                    <input 
                         class="block w-full px-4 py-3 bg-transparent border-2 border-zinc-700 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 text-white placeholder-gray-400 transition-all duration-300 ease-in-out" 
                         type="text" 
                         placeholder="Username" 
                         name="username"
                         required
                    >
               </div>

               <!-- Email Field -->
               <div class="relative">
                    <input 
                         class="block w-full px-4 py-3 bg-transparent border-2 border-zinc-700 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 text-white placeholder-gray-400 transition-all duration-300 ease-in-out" 
                         type="email" 
                         placeholder="Email" 
                         name="email"
                         required
                    >
               </div>

               <!-- Age Field -->
               <div class="relative">
                    <input 
                         class="block w-full px-4 py-3 bg-transparent border-2 border-zinc-700 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 text-white placeholder-gray-400 transition-all duration-300 ease-in-out" 
                         type="number" 
                         placeholder="Age" 
                         name="age"
                         required
                    >
               </div>

               <!-- Password Field -->
               <div class="relative">
                    <input 
                         class="block w-full px-4 py-3 bg-transparent border-2 border-zinc-700 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 text-white placeholder-gray-400 transition-all duration-300 ease-in-out" 
                         type="password" 
                         placeholder="Password" 
                         name="password"
                         required
                    >
               </div>

               <!-- Submit Button -->
               <div>
                    <button 
                         type="submit" 
                         class="w-full py-3 px-4 text-lg font-semibold text-white bg-gradient-to-r from-blue-500 to-purple-600 rounded-lg shadow-lg hover:from-blue-600 hover:to-purple-700 focus:outline-none focus:ring-4 focus:ring-blue-500 focus:ring-opacity-50 transition-all duration-300 ease-in-out"
                    >
                         Create User
                    </button>
               </div>
          </form>

          <!-- Login Link -->
          <div class="text-center mt-6">
               <a href="/login" class="font-medium text-indigo-500 hover:text-indigo-400 transition-all duration-300 ease-in-out">
                    Already have an account? Login
               </a>
          </div>
     </div>
</body>
</html>
